__pycache__/
*.pyc

# Registry publish artifact (created by `comfy node pack` / `comfy node publish`)
node.zip
*.zip

# Design notes stay local
docs/

# Tests stay local (the repo is the custom node folder)
tests/
